version: '3.4'

services:
 gateway.web.api:
    image: ${DOCKER_REGISTRY-}gateway-web-api
    build:
      context: .
      dockerfile: src/Gateways/Gateway.Web.Api/Dockerfile
    container_name: gateway-web-api
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - TZ=Europe/Khmelnytskyi
    ports:
      - "11000:8080"

 postgres-db:
    image: postgres:14
    container_name: postgres-db
    restart: always
    ports:
      - "10100:5432"
    environment:
      POSTGRES_PASSWORD: 123258

 user.authentication.api:
    image: ${DOCKER_REGISTRY-}user-authentication-api
    build:
      context: .
      dockerfile: src/Services/User/Authentication/User.Authentication.Api/Dockerfile
    container_name: user-authentication-api
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
#      - ConnectionStrings__DefaultConnection=User ID=postgres;Password=123258;Server=postgres-db;Port=5432;Database=user-authentication-server;Pooling=true;
      - TZ=Europe/Khmelnytskyi
    ports:
      - "10101:8080"
    depends_on:
      - postgres-db
      - user.authentication.server

 user.authentication.server:
    image: ${DOCKER_REGISTRY-}user-authentication-server
    build:
      context: .
      dockerfile: src/Services/User/Authentication/User.Authentication.Server/Dockerfile
    container_name: user-authentication-server
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
#      - DOTNET_RUNNING_IN_CONTAINER=true
#      - ConnectionStrings__DefaultConnection=User ID=postgres;Password=123258;Server=postgres-db;Port=5432;Database=user-authentication-server;Pooling=true;
      - TZ=Europe/Khmelnytskyi
    ports:
      - "10102:8080"
    depends_on:
      - postgres-db

 shifttrack.api:
    image: ${DOCKER_REGISTRY-}shifttrack-api
    build:
      context: .
      dockerfile: src\Services\ShiftTrack\ShiftTrack.API\Dockerfile
    container_name: shifttrack-api
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      - TZ=Europe/Khmelnytskyi
    ports:
      - "20001:8080"

 generators.pdf.api:
   image: ${DOCKER_REGISTRY-}generators-pdf-api
   build:
     context: .
     dockerfile: src/Services/Generators/Pdf/Generators.Pdf.Api/Dockerfile
   container_name: generators-pdf-api
   environment:
     - DOTNET_RUNNING_IN_CONTAINER=true
     - DOTNET_USE_POLLING_FILE_WATCHER=true
     - TZ=Europe/Khmelnytskyi
     - Puppeteer__ExecutablePath=/usr/bin/google-chrome-stable
   restart: always
   ports:
     - "20002:8080"
   
 location.api:
   image: ${DOCKER_REGISTRY-}location-api
   build:
     context: .
     dockerfile: src/Services/Location/Location.Api/Dockerfile
   container_name: location-api
   restart: always
   environment:
     - ASPNETCORE_ENVIRONMENT=Development
     - ASPNETCORE_URLS=http://+:8080
     - TZ=Europe/Khmelnytskyi
   ports:
     - "20003:8080"